name: Daily BuenaVista Post

on:
  schedule:
    # Runs at 4:30 PM IST (11:00 AM UTC) every day
    - cron: "0 11 * * *"
  workflow_dispatch: # Allow manual trigger from GitHub UI

jobs:
  post:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Post to Bluesky
        env:
          OTM_API_KEY: ${{ secrets.OTM_API_KEY }}
          CLAUDE_API_KEY: ${{ secrets.CLAUDE_API_KEY }}
          BLUESKY_HANDLE: ${{ secrets.BLUESKY_HANDLE }}
          BLUESKY_APP_PASSWORD: ${{ secrets.BLUESKY_APP_PASSWORD }}
          UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}
        run: python post_to_bluesky.py

      - name: Post to X
        env:
          OTM_API_KEY: ${{ secrets.OTM_API_KEY }}
          CLAUDE_API_KEY: ${{ secrets.CLAUDE_API_KEY }}
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
          UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}
        run: python post_to_x.py
        continue-on-error: true  # Don't fail the workflow if X is blocked

      - name: Commit new location files
        run: |
          git config user.name "BuenaVista Bot"
          git config user.email "bot@buenavista.in"
          git add DailyViews/
          git diff --cached --quiet || git commit -m "🌍 Daily location: $(date +%Y-%m-%d)"
          git push
